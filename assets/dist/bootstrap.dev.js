"use strict";

var _stimulusBundle = require("@symfony/stimulus-bundle");

var app = (0, _stimulusBundle.startStimulusApp)(); // register any custom, 3rd party controllers here
// app.register('some_controller_name', SomeImportedController);